<%- cache(element) do -%>
  <%= element_view_for(element) do |el| -%>
    <% image = el.essence(:background_image) %>
    <% video = el.ingredient(:video) %>
    <% headline = el.ingredient(:headline) %>
    <% color = el.ingredient(:text_color) %>

    <div class="bg-image" style="background: url(<%= image.picture_url %>) no-repeat center center; background-size: cover;">

      <%if video %>
        <video id="bgvideo" preload="auto" autoplay loop>
          <source src=" <%= video %>" type="video/mp4"></source>
          <!-- <source src="https://s3-us-west-2.amazonaws.com/nytimesvideos/NYTnow_loop.webm" type="video/webm"></source> -->
        </video>
      <%end%>

      <%if !headline.blank? %>
        <h2 class="hero-headline <%=color%>"><%= headline %></h2>
      <% end %>

        <!-- Gradient fill -->
<!--         <div id="svg-product-hero">
              <svg width ="100%" height="100%" version="1.1" preserveAspectRatio="xMinYMin meet" viewBox="0 100  50 20" class="svg_content">
            <defs>
            <linearGradient id = "gradient" x = "0%" y = "100%" width = "100%" height = "200">
              <stop stop-color = "#fff" offset = "0%"/>
              <stop stop-color = "#a58e68" offset = "50%"/>
              <stop stop-color = "#554035" offset = "100%"/>
            </linearGradient>
            </defs>
            <text x="50%" y="50%" font-size="180" fill="url(#gradient)" height="150" text-anchor="middle"><%#= el.render :headline%></text>
          </svg>
        </div> -->


    </div>

  <%- end -%>
<%- end -%>
